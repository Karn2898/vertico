[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vertico-sandbox"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["docker"]

[tool.setuptools]
packages = ["docker", "executors", "policies"]
include-package-data = true